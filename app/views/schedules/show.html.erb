<% if @relationship.is_admin == true %>
  <div class="content ja-txt">
    <div class="row">
      <div class="col-sm-8 offset-sm-2">
        <div class="card mb-3">
          <% if (@schedule.start_time.strftime("%Y年%m月%d日") == @schedule.end_time.strftime("%Y年%m月%d日")) %>
            <div class="card-header">
              <h2 class="text-center">予定詳細</h2>
            </div>
            <div class="card-body">
              <h4 class="en-txt">Title : <span class="font-weight-normal ja-txt"><%= @schedule.title %></span></h4>
              <h4 class="en-txt">Date : <span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%Y年%m月%d日") %></span></h4>
              <h4 class="en-txt">Time：<span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%H:%M") %> 〜 <%= @schedule.end_time.strftime("%H:%M") %></span></h4>
              <h4 class="en-txt">Place：<span class="font-weight-normal ja-txt"><%= @schedule.place %></span></h4>
              <h4 class="en-txt">Memo：<span class="font-weight-normal ja-txt"><%= @schedule.detail %></span></h4>
            </div>
          <% else %>
            <div class="card-header">
              <h2 class="text-center">予定詳細</h2>
            </div>
            <div class="card-body">
              <h4 class="en-txt">Title : <span class="font-weight-normal ja-txt"><%= @schedule.title %></span></h4>
              <h4 class="en-txt">Date : <span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%Y年%m月%d日") %> 〜 <%= @schedule.end_time.strftime("%Y年%m月%d日") %></span></h4>
              <h4 class="en-txt">Time：<span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%m月%d日 %H:%M") %> 〜 <%= @schedule.end_time.strftime("%m月%d日 %H:%M") %></span></h4>
              <h4 class="en-txt">Place：<span class="font-weight-normal ja-txt"><%= @schedule.place %></span></h4>
              <h4 class="en-txt">Memo：<span class="font-weight-normal ja-txt"><%= @schedule.detail %></span></h4>
            </div>
          <% end %>
        </div>
        
        <div class="d-flex justify-content-center">
            <%= link_to '予定を編集', edit_group_schedule_path(@group, @schedule), class: 'btn btn-lg btn-primary my-3 mx-4' %>
            <%= link_to '予定を削除', @destroyed_schedule, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-lg btn-primary my-3 mx-4' %>
        </div>
        <%= link_to 'グループトップへ戻る', group_schedules_path(@group) %>
      </div>
    </div>
  </div>
<% else %>
  <div class="content ja-txt">
    <div class="row">
      <div class="col-sm-8 offset-sm-2">
        <div class="card mb-3">
          <% if (@schedule.start_time.strftime("%Y年%m月%d日") == @schedule.end_time.strftime("%Y年%m月%d日")) %>
            <div class="card-header">
              <h2 class="text-center">予定詳細</h2>
            </div>
            <div class="card-body">
              <h4 class="en-txt">Title : <span class="font-weight-normal ja-txt"><%= @schedule.title %></span></h4>
              <h4 class="en-txt">Date : <span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%Y年%m月%d日") %></span></h4>
              <h4 class="en-txt">Time：<span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%H:%M") %> 〜 <%= @schedule.end_time.strftime("%H:%M") %></span></h4>
              <h4 class="en-txt">Place：<span class="font-weight-normal ja-txt"><%= @schedule.place %></span></h4>
              <h4 class="en-txt">Memo：<span class="font-weight-normal ja-txt"><%= @schedule.detail %></span></h4>
            </div>
          <% else %>
            <div class="card-header">
              <h2 class="text-center">予定詳細</h2>
            </div>
            <div class="card-body">
              <h4 class="en-txt">Title : <span class="font-weight-normal ja-txt"><%= @schedule.title %></span></h4>
              <h4 class="en-txt">Date : <span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%Y年%m月%d日") %> 〜 <%= @schedule.end_time.strftime("%Y年%m月%d日") %></span></h4>
              <h4 class="en-txt">Time：<span class="font-weight-normal ja-txt"><%= @schedule.start_time.strftime("%m月%d日 %H:%M") %> 〜 <%= @schedule.end_time.strftime("%m月%d日 %H:%M") %></span></h4>
              <h4 class="en-txt">Place：<span class="font-weight-normal ja-txt"><%= @schedule.place %></span></h4>
              <h4 class="en-txt">Memo：<span class="font-weight-normal ja-txt"><%= @schedule.detail %></span></h4>
            </div>
          <% end %>
        </div>
        
        <%= link_to 'グループトップへ戻る', group_schedules_path(@group) %>
      </div>
    </div>
  </div>
<% end %>